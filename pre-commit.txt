#==============================================================================
$Id$
#==============================================================================
#!/usr/bin/env bash
 
for file in `git diff-index --name-only --diff-filter=AM HEAD` ; do
perl -pi -e 's/(\$[ \t]*Id)(.*)([ \t]*\$)/"\$Id$"/e' ${file}
        git add ${file} > /dev/null
done

